fun main(args: Array<String>) {

    // If as an expression fun bigger

    // For loop with list

    // When expression | fun numberType | 4 thinks + else

    // When expression with predicates | > < = | signAsString 

}